<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Basic Page Needs
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <meta charset="utf-8">
  <title>{{ page.title | replace: "<br>"," "}}</title>
  <meta name="description" content="JD Gross Lab at UCSF">
  <meta name="author" content="Ben Barad @benjaminbarad">

  <!-- CSS
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <link type="text/css" rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.0/css/materialize.min.css" media="screen, projection">
   <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link type="text/css" rel="stylesheet" href="{{site.baseurl}}/static/css/custom.css" media="screen, projection">

  <!--Let browser know website is optimized for mobile-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>

  <!-- Favicon
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <link rel="icon" type="image/ico" href="{{site.baseurl}}/static/img/favicon.ico">

</head>
<body>
  <nav class="black" role="navigation">
    <div class="nav-wrapper container">
      <a id="logo-container" href="#" class="brand-logo">JD Gross Lab</a>
      <ul class="right hide-on-med-and-down">
        {% for link in site.data.navbar %}
          {% if page.group == link.group %}
            <li class = "active waves-effect"><a href="{{ site.baseurl }}{{link.link}}" alt="{{link.name}}"> {{link.name}}</a></li>
          {% else %}
            <li class="waves-effect"><a href="{{ site.baseurl }}{{link.link}}" alt="{{link.name}}"> {{link.name}}</a></li>
          {% endif %}
        {% endfor %}
      </ul>
      <a href="#" data-activates="nav-mobile" class="button-collapse"><i class="mdi-navigation-menu"></i></a>
    </div>
  </nav>

  <ul id="nav-mobile" class="side-nav">
    <li class="logo"><a id="logo-container" href="{{site.baseurl}}/" class="brand-logo">
        <img id="front-page-logo" src="{{ site.baseurl }}/static/img/sidenav.png"></img>
    </a></li>
    {% for link in site.data.navbar %}
      {% if page.group == link.group %}
        <li class="bold active "><a href="{{ site.baseurl }}{{link.link}}" alt="{{link.name}}"> {{link.name}}</a></li>
      {% else %}
        <li class="bold"><a href="{{ site.baseurl }}{{link.link}}" alt="{{link.name}}"> {{link.name}}</a></li>
      {% endif %}
    {% endfor %}
  </ul>


